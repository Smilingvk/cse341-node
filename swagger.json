{
  "openapi": "3.0.3",
  "info": {
    "title": "Users and Products API",
    "version": "1.0.0",
    "description": "API para gestionar usuarios y productos con MongoDB. Para autenticarte, visita /auth/login en tu navegador."
  },
  "servers": [
    {
      "url": "https://cse341-node-7yaa.onrender.com",
      "description": "Servidor de producción"
    },
    {
      "url": "http://localhost:3000",
      "description": "Servidor local"
    }
  ],
  "paths": {
    "/auth/login": {
      "get": {
        "summary": "Login con GitHub OAuth (abrir en navegador)",
        "description": "Este endpoint debe abrirse en un navegador, no puede ejecutarse desde Swagger UI. Visita https://cse341-node-7yaa.onrender.com/auth/login",
        "tags": ["Authentication"],
        "responses": {
          "302": { "description": "Redirige a GitHub para autenticación" }
        }
      }
    },
    "/auth/logout": {
      "get": {
        "summary": "Logout (abrir en navegador)",
        "description": "Cierra la sesión. Debe abrirse en un navegador.",
        "tags": ["Authentication"],
        "responses": {
          "302": { "description": "Cierra sesión y redirige" }
        }
      }
    },
    "/users": {
      "get": {
        "summary": "Obtener todos los usuarios",
        "tags": ["Users"],
        "responses": {
          "200": {
            "description": "Lista de usuarios",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": { "$ref": "#/components/schemas/User" }
                }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Crear un nuevo usuario (requiere autenticación)",
        "description": "Primero debes autenticarte visitando /auth/login en tu navegador, luego regresa aquí para usar este endpoint",
        "tags": ["Users"],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": { "$ref": "#/components/schemas/User" }
            }
          }
        },
        "responses": {
          "201": { "description": "Usuario creado exitosamente" },
          "400": { "description": "Faltan campos requeridos" },
          "401": { 
            "description": "No autenticado - Visita /auth/login primero",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "error": { "type": "string", "example": "Authentication required. Please log in." }
                  }
                }
              }
            }
          },
          "500": { "description": "Error interno del servidor" }
        }
      }
    },
    "/users/{id}": {
      "get": {
        "summary": "Obtener un usuario por ID",
        "tags": ["Users"],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": { "type": "string" },
            "description": "ID del usuario"
          }
        ],
        "responses": {
          "200": { 
            "description": "Usuario encontrado", 
            "content": { 
              "application/json": { 
                "schema": { "$ref": "#/components/schemas/User" } 
              } 
            } 
          },
          "404": { "description": "Usuario no encontrado" }
        }
      },
      "put": {
        "summary": "Actualizar un usuario por ID (requiere autenticación)",
        "description": "Primero debes autenticarte visitando /auth/login en tu navegador",
        "tags": ["Users"],
        "parameters": [
          { 
            "name": "id", 
            "in": "path", 
            "required": true, 
            "schema": { "type": "string" }, 
            "description": "ID del usuario" 
          }
        ],
        "requestBody": {
          "required": true,
          "content": { 
            "application/json": { 
              "schema": { "$ref": "#/components/schemas/User" } 
            } 
          }
        },
        "responses": {
          "200": { "description": "Usuario actualizado" },
          "400": { "description": "Datos inválidos" },
          "401": { "description": "No autenticado - Visita /auth/login primero" },
          "404": { "description": "Usuario no encontrado" }
        }
      },
      "delete": {
        "summary": "Eliminar un usuario por ID (requiere autenticación)",
        "description": "Primero debes autenticarte visitando /auth/login en tu navegador",
        "tags": ["Users"],
        "parameters": [
          { 
            "name": "id", 
            "in": "path", 
            "required": true, 
            "schema": { "type": "string" }, 
            "description": "ID del usuario" 
          }
        ],
        "responses": {
          "204": { "description": "Usuario eliminado" },
          "401": { "description": "No autenticado - Visita /auth/login primero" },
          "404": { "description": "Usuario no encontrado" }
        }
      }
    },
    "/products": {
      "get": {
        "summary": "Obtener todos los productos",
        "tags": ["Products"],
        "responses": {
          "200": {
            "description": "Lista de productos",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": { "$ref": "#/components/schemas/Product" }
                }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Crear un nuevo producto (requiere autenticación)",
        "description": "Primero debes autenticarte visitando /auth/login en tu navegador",
        "tags": ["Products"],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": { "$ref": "#/components/schemas/Product" }
            }
          }
        },
        "responses": {
          "201": { "description": "Producto creado exitosamente" },
          "400": { "description": "Faltan campos requeridos" },
          "401": { "description": "No autenticado - Visita /auth/login primero" },
          "500": { "description": "Error interno del servidor" }
        }
      }
    },
    "/products/{id}": {
      "get": {
        "summary": "Obtener un producto por ID",
        "tags": ["Products"],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": { "type": "string" },
            "description": "ID del producto"
          }
        ],
        "responses": {
          "200": { 
            "description": "Producto encontrado", 
            "content": { 
              "application/json": { 
                "schema": { "$ref": "#/components/schemas/Product" } 
              } 
            } 
          },
          "404": { "description": "Producto no encontrado" }
        }
      },
      "put": {
        "summary": "Actualizar un producto por ID (requiere autenticación)",
        "description": "Primero debes autenticarte visitando /auth/login en tu navegador",
        "tags": ["Products"],
        "parameters": [
          { 
            "name": "id", 
            "in": "path", 
            "required": true, 
            "schema": { "type": "string" }, 
            "description": "ID del producto" 
          }
        ],
        "requestBody": {
          "required": true,
          "content": { 
            "application/json": { 
              "schema": { "$ref": "#/components/schemas/Product" } 
            } 
          }
        },
        "responses": {
          "200": { "description": "Producto actualizado" },
          "400": { "description": "Datos inválidos" },
          "401": { "description": "No autenticado - Visita /auth/login primero" },
          "404": { "description": "Producto no encontrado" }
        }
      },
      "delete": {
        "summary": "Eliminar un producto por ID (requiere autenticación)",
        "description": "Primero debes autenticarte visitando /auth/login en tu navegador",
        "tags": ["Products"],
        "parameters": [
          { 
            "name": "id", 
            "in": "path", 
            "required": true, 
            "schema": { "type": "string" }, 
            "description": "ID del producto" 
          }
        ],
        "responses": {
          "204": { "description": "Producto eliminado" },
          "401": { "description": "No autenticado - Visita /auth/login primero" },
          "404": { "description": "Producto no encontrado" }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "User": {
        "type": "object",
        "properties": {
          "firstName": { "type": "string", "example": "Juan" },
          "lastName": { "type": "string", "example": "Perez" },
          "email": { "type": "string", "example": "juan@example.com" },
          "favoriteColor": { "type": "string", "example": "blue" },
          "birthday": { "type": "string", "format": "date", "example": "1990-01-01" }
        },
        "required": ["firstName", "lastName", "email"]
      },
      "Product": {
        "type": "object",
        "properties": {
          "name": { "type": "string", "example": "Laptop Dell XPS 15" },
          "description": { "type": "string", "example": "High-performance laptop with 16GB RAM" },
          "price": { "type": "number", "example": 1299.99 },
          "category": { "type": "string", "example": "Electronics" },
          "stock": { "type": "number", "example": 25 },
          "manufacturer": { "type": "string", "example": "Dell" },
          "releaseDate": { "type": "string", "format": "date", "example": "2024-01-15" }
        },
        "required": ["name", "description", "price", "category"]
      }
    }
  }
}